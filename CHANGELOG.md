## 0.1.0

- Unreleased. Establishes the cross-platform `Autostart` surface, platform
  dispatch, and the shared exception hierarchy. No platform backend is wired up
  yet, so every operating system is served by the unsupported backend.
